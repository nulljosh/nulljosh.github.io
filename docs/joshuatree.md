# Architecture

What each file does and how boot actually proceeds. `docs/roadmap.md` is the plan;
this is the map of what exists right now.

## Boot sequence

Higher-half kernel: everything from `kmain` on runs at 0xC0000000+, loaded
physically at 1MB. `boot/boot.S`'s `_start` (deliberately unrelocated, it
runs both before and after paging turns on) builds a temporary page
directory mapping the first 4MB both identity and at the high base, turns
on paging, jumps into the real kernel. `paging_install()` then replaces
that with the permanent, same-shaped tables.

1. `boot/boot.S`: multiboot1 header, temporary page tables, paging on,
   jump to the higher-half-linked `higher_half_entry`, which sets the real
   stack, pushes the multiboot info pointer GRUB/QEMU left in `%ebx`, calls
   `kmain`.
2. `kmain` (`kernel/kernel.c`) brings subsystems up in dependency order:
   `gdt_install` → `idt_install` → `irq_install` → `pmm_init` →
   `paging_install` → `tasks_init` → `fat_mount` → the shell loop.

## Subsystems

| File | What it owns |
|---|---|
| `gdt.c` | Flat GDT: ring-0 and ring-3 code/data segments (both spanning 4GB) plus a TSS for ring-3-to-ring-0 stack switches |
| `idt.c` + `isr.S` | IDT, the 32 CPU-exception handlers, and the `int 0x80` entry. A ring-0 exception prints and halts (a kernel bug); a ring-3 one names itself, reaps the task, and the kernel keeps running (v64). If the GUI is active, a ring-0 exception also paints a readable panic screen showing the exception name, fault address (for page faults), EIP, kernel version, and a two-line system message before halting (v0.99.2) |
| `syscall.c` | `int 0x80` dispatch table, x86 Linux convention and numbers. The v1 set: `exit` (1), `read` (3), `write` (4), `open` (5), `close` (6), `time` (13), `getpid` (20), `sched_yield` (158), plus the per-task file-descriptor table `open` hands out of. v2 adds `lseek` (19), `open` flags (`O_WRONLY`/`O_RDWR`/`O_CREAT`/`O_TRUNC`/`O_APPEND`) and `write` to a descriptor from `open`. A writable descriptor is the read snapshot run backwards: the whole file is buffered at `open`, `write` edits the buffer, `close` hands it back through `vfs_replace_file()` and then reads it straight back to check the bytes landed, because the VFS has no partial-write primitive and ramfs truncates past 4096 bytes while still reporting success. Every user pointer is checked with `paging_user_range_ok` before the kernel touches it, every copy is bounded, every failure is a negative errno. `docs/SYSCALL-ABI.md` is the contract (v1 at v64, frozen; v2 alongside `user/note.c`) |
| `pic.c` | Remaps the 8259 PIC so IRQs land on vectors 32-47 instead of overlapping CPU exceptions |
| `irq.c` + `irq_stubs.S` | IRQ0 (PIT tick counter) and IRQ1 (keyboard ring buffer) |
| `pmm.c` | Physical memory: a bitmap over `mem_upper` from the multiboot info struct |
| `paging.c` | Permanent page tables: identity-maps the first 4MB and double-maps it at 0xC0000000 for the kernel's own higher-half code/data |
| `kheap.c` | `kmalloc`/`kfree`, a first-fit free list grown a frame at a time from `pmm.c` |
| `task.c` + `irq_stubs.S`'s irq0 | Preemptive round-robin off the PIT tick. `yield()` reaches the same switch in software via `int $32`, same IDT gate as the hardware timer. Ring-3 tasks sit in the same table since v64 (`task_create_user`), each with its own kernel stack that the TSS `esp0` is repointed to on every switch; the saved frame carries DS/ES/FS/GS so a resume into ring 3 keeps its own selectors |
| `ring3.c` + `ring3_asm.S` | Ring-3 privilege isolation and user-mode task execution. Copies a payload onto a user page and runs it at CPL 3 with restricted instruction set (privileged instructions fault). Since v64, user tasks sit in the same task table as kernel tasks (`task_create_user`), with `int 0x80` as the syscall gate to write/exit. `ring3test` / `ring3test fault` / `ring3test spin` verify the three cases: normal syscall, privileged-instruction fault, and preemption on a ring-3 task (v3 / v64) |
| `ata.c` | ATA PIO disk driver, LBA28, primary master only |
| `fat.c` | FAT16, real subdirectories and file writes, 8.3 names |
| `exec.c` | Loads a flat binary off the VFS into the fixed window `boot/linker.ld` reserves at `0xC0500000`, marks its eight pages user-accessible, and runs it as a real ring-3 task via `task_create_user`. v2 also builds the program's initial stack: a fake return address, `argc` and `argv` in the i386 cdecl frame a plain C `_start(int, char **)` already expects, so a flat binary gets real arguments with no crt0 and no assembly. Was `exec_flat`, which called the same kind of binary at ring 0 with kernel privileges, so the syscall ABI was never actually exercised by it |
| `user/hello.c` + `user/jtsys.h` + `user/hello.ld` | The v1 ring-3 reference program and the only header a user program gets: inline `int $0x80` wrappers, no libc, no kernel include path. Built by the Makefile into a flat binary, embedded in the kernel so `usertest` can seed it through the VFS on a disk-less boot, then loaded back off the VFS and run at CPL 3. This is the external consumer that makes `docs/SYSCALL-ABI.md` a contract |
| `user/note.c` + `user/note.ld` | The v2 reference program, and unlike `hello` a program someone would run rather than an interface self-test: `note FILE` prints a file, `note FILE text...` appends a line to it, `note FILE @N text...` seeks into the middle and overwrites. It is the only thing in this repo that can change a file from ring 3, so it is what proves v2's write path, its `lseek` and its `argv` are real. `notetest` runs it three times with three argv vectors and then reads the file back off the VFS itself to check the bytes |
| `drivers/vfs.c` | v29's real VFS: a `vfs_ops` table (`read_file`/`list`/`delete_`/`chdir`/`mkdir`/`write_file`/`replace_file`) so callers stop dialing `fat_*` directly, correctly deferred at v4 until a second backend actually existed to abstract over. `vfs_switch()` (the `fsuse` shell command) flips the active backend; `fat.c` and `ramfs.c` each register one at boot, first-registered wins by default |
| `drivers/blockdev.c` | v33's block device abstraction, the same "second implementation, not an imagined future one" reasoning one layer down: a `blockdev_ops` table of `read_sector`/`write_sector` so `fat.c` no longer calls `ata_*` directly. `ata.c` and `ramdisk.c` each register a backend at boot |
| `drivers/ramfs.c` + `drivers/ramdisk.c` | The real second implementations `vfs.c` and `blockdev.c` needed to prove each abstraction actually abstracts something. `ramfs.c`: 8 files, 4KB each, a flat namespace on purpose (`chdir`/`mkdir` honestly return failure, no subdirectory story exists). `ramdisk.c`: 256 512-byte sectors (128KB) of plain zeroed RAM, deliberately small, this proves the interface, it isn't meant to hold real data. v86 (0.71.0): `kmain` (kernel.c) now seeds `README.TXT`/`NOTES.TXT` into ramfs and `vfs_switch("ramfs")`s the active backend whenever `fat_mount()` reports no disk, the real condition v86's browser boot always hits (no disk image is ever wired into its multiboot path), fixing the landing-page Files app showing "no files"; native boots with a real FAT disk are unaffected since this only fires when `fat_mount()` already failed |
| `drivers/trash.c` | v39's real, recoverable delete: before this, `rm` went straight to FAT's own `0xE5`-marks-the-entry removal, which doesn't even free the clusters (see `fat.c`'s own note), bytes gone with no way back. `trash_put` copies a file's bytes into an 8-item, 4KB-each RAM table before the caller deletes it; `trash_restore` writes it back through `vfs_write_file` and only drops the RAM copy if that write actually succeeds, so a failed restore never loses the file twice. RAM-only and gone on reboot, an honest tradeoff against a disk-backed trash's own reserved-directory and collision-naming problems, neither of which this kernel has an answer for yet |
| `drivers/pci.c` | Legacy 0xCF8/0xCFC PCI config-space enumeration: an exhaustive bus/slot/function scan (`pci_find_device`, QEMU's device tree is tiny enough that this costs microseconds) plus the bus-master/IO/memory enable bits (`pci_enable_device`). Shared foundation under two later subsystems: v6's graphics (finds the VBE framebuffer's BAR0) and v7's networking (finds the RTL8139's I/O BAR) |
| `drivers/rtl8139.c` | The real NIC driver behind v7's networking: register-level reset/init, one RX ring buffer (polled on the Command Register's BUFE bit since v71 / 0.65.0, the OSDev/Linux `RxBufEmpty` recipe; the old ISR-ROK gate ran one packet behind after any two-frame burst, hidden for sixty versions because every caller reset the NIC before its single connection, exposed the day `weather_fetch` made two), and 4 separate TX descriptor buffers cycled in order (reusing one descriptor for every send works exactly once, then stalls the card forever, found via a real pcap capture, not reasoning). Needed v2's higher-half fix (`KVIRT_TO_PHYS`) since the card DMAs to physical addresses with no concept of the CPU's page tables at all, a bug MMIO register reads (the MAC readout) never exposed, only an independent `tcpdump` capture of all-zero frames did. Only NIC v7 originally had a driver for; targets real hardware and native QEMU's `-device rtl8139`, but v86 (the landing page's in-browser emulator) never emulates this chip at all (confirmed by grep, zero hits in `landing/v86/libv86.js`), so the browser demo's wallpaper fetch always silently fell back to the static photo until `ne2k.c` shipped |
| `drivers/ne2k.c` | v0.73.0's NE2000/RTL8029 driver, the second NIC backend `net.c` probes for when no RTL8139 is present, specifically to unlock v86's browser demo (v86 emulates an NE2000-compatible card, PCI vendor 0x10EC device 0x8029, confirmed against `landing/v86/libv86.js`'s own `pci_space` table). Page-addressed ring buffer inside the card's own internal memory (no host-RAM DMA at all, a real structural difference from `rtl8139.c`), remote-DMA read/write protocol for both the one-time PROM MAC read and every RX/TX transfer. Two real bugs found live, headless, against v86, neither guessable from the datasheet alone: (1) DCR's word-transfer bit (set here, matching v86's own accurate emulation of real word-mode 8390 hardware) means every data-port access, even an explicit byte-sized one, actually moves two bytes and advances the remote-DMA pointer by 2, not 1; single-byte `inb`/`outb` on the data port silently corrupted and desynced every PROM/packet read until switched to `inw`/`outw`. (2) The RX ring's oldest unread packet lives at page `BNRY + 1`, never at `BNRY` itself (standard 8390 semantics, confirmed against v86's own `Mc.prototype.receive`, which writes each incoming packet at the *pre-advance* `CURR` page); reading from `BNRY` directly reported "ring non-empty" from the very first poll (before anything had arrived) and read an all-zero unused page, silently discarding the real ARP reply that landed one page later. Same length-clamp discipline as `rtl8139_receive`'s own fixed OOB bug (`ne2k_clamp_len`, unit-tested by `ne2k_clamp_selftest`) |
| `drivers/net.c` | Ethernet/ARP/IPv4/UDP/TCP built up from raw frames, hardware-agnostic over whichever NIC `net_init` finds (`rtl8139.c` first, `ne2k.c` as fallback, via an `active_send`/`active_receive` function-pointer pair). Deliberately narrow: no routing table (assumes every destination is on the same /24 link, true for QEMU's SLIRP), no fragmentation, `tcp_get`/`tcp_serve_once` are one connection at a time with no retransmission or out-of-order reassembly. Extended post-v9 to chunk a response across multiple 536-byte segments stop-and-wait (the original single-segment cap silently truncated real pages); v10 replaced every wait's loop-iteration guess with a real `ticks()`-based deadline after proving under host load that a spin count isn't a real timeout; v30 added `tcp_probe_port`, a short-timeout SYN probe behind the `netscan` hacking-tools command. v75: `tcp_get` takes a fresh local port per connection (44000..59999, wrapping) and varies its ISN by it; a fixed port 44000 was fine while every caller spoke to a different host once per boot, but the map wallpaper's twelve back-to-back connections to one server hit a 4-tuple SLIRP still held half-closed (this side FINs and never waits for the last ACK, by design), reproducibly failing on the fourth. v0.73.0: `net_init` now owns NIC discovery itself (RTL8139 then NE2000), replacing the old two-step `rtl8139_init()` then `net_init(ip)` every call site used to need |
| `drivers/http.c` + `drivers/html.c` + `drivers/json.c` | v8's browser layer, mostly glue over v6+v7, the HTML parser deliberately tiny. `http.c`: `http_get`/`http_post` build a request over `net.c`'s `tcp_get` and strip the reply down to the body past the first blank line. v75: `http_get`'s reply buffer is kmalloc'd at `body_maxlen + 2048`, replacing a fixed 2KB stack buffer that silently capped every reply (headers included) at 2047 bytes, fine for JSON one-liners, hopeless for a 40KB map tile. `html.c`: `html_to_text` strips tags (skipping `<script>`/`<style>` element *content* instead of printing it as page text, a real bug found live against example.com's own stylesheet) and unescapes the five basic entities; bytes above 0x80 are dropped rather than mis-rendered, a real bug (multi-byte UTF-8 landing on CP437's line-drawing glyphs, reading as a screen-wide stripe pattern) caught the same way. `json.c`: `json_extract_string` is a key-value grabber, not a parser, string values only, no arrays/nesting, enough to read one field out of an LLM API's JSON reply; `json_extract_number_text` (v71 / 0.65.0) is its numeric sibling, copying a bare number's exact text (`"lat":49.0983` -> `49.0983`) so `weather_fetch` can splice ip-api.com's coordinates straight into its Open-Meteo URL with no float round trip |
| `drivers/vbe.c` | The Bochs VBE register interface (ports 0x1CE/0x1CF), the only path QEMU's std VGA device actually exposes to a 32-bit kernel with no real-mode BIOS monitor. `vbe_set_mode`/`vbe_disable` switch graphics on and off at runtime, saving and restoring the real VGA Sequencer/CRTC/GC/Attribute-Controller register state around the switch (a real v6/v8 bug: `vbe_disable` alone left those registers in a graphics-mode state, breaking the shell it was supposed to return to). `vga_text_mode_init` (v16) programs mode 3 and the 16-color DAC palette from first principles, since v86 (no BIOS) never sets either up the way real hardware/QEMU do for free. `vbe_set_mode` matches PCI vendor:device `1234:1111` exactly (`pci_find_device_vid`), not just "any VGA-class device" (ISO-boot audit, real-hardware pass): a Cirrus/VMware-SVGA/real-GPU VGA device still matches PCI class 0x03/0x00 but doesn't implement the Bochs DISPI registers, so the old broad match would poke nothing, hand back a BAR0 that isn't the framebuffer it just asked for, and let `window_open_scaled` map and write into it anyway. Failing the match cleanly instead makes `gui_run`'s existing `if (!window_open_scaled(...))` check do the graceful thing: boot to serial/text with no crash, no garbage framebuffer, no graphics, on any machine that isn't running a Bochs/QEMU-compatible virtual VGA adapter |
| `tools/iso/limine.conf` + `make iso` (Makefile) | ISO/USB boot path for real machines and other VMs, alongside the existing `-kernel` path QEMU alone can take. Fetches [Limine](https://github.com/limine-bootloader/limine)'s prebuilt binaries into a gitignored `build/limine` at build time (never committed, no brew/apt install needed beyond `xorriso`), packs `kernel.elf` with them into a hybrid BIOS+UEFI ISO that is also a valid raw USB disk image (isohybrid MBR, `limine bios-install` on the xorriso output). `boot/boot.S` needed no changes: Limine's `protocol: multiboot1` loads `kernel.elf` exactly like QEMU's own `-kernel` does (`eax`=multiboot magic, `ebx`=multiboot info pointer). No disk attached still boots straight to the desktop (`kmain`'s existing ramfs fallback, the same path the no-disk v86 browser demo already exercises) |
| `drivers/font.c` | Dumps the real IBM CP437 8x16 font out of VGA hardware plane 2 (the OSDev-wiki technique), not hand-authored glyph bitmaps. Falls back to an embedded table (`vgafont.h`) when the dump comes back all zero (v38: v86 has no BIOS to have loaded a font for it to steal). `font_set_aa`'s hook (v44) lets the GUI swap this bitmap path for a real antialiased DejaVu Sans/Serif/Mono render at physical resolution wherever a scaled framebuffer exists. Since v77 (0.67.1) the AA path lays strings out with a real pen advanced by each glyph's own table metric (`font_set_aa` takes a second advance hook), the fix for the photographed "Cl oudy" menu-bar gap that the old fixed 16px-per-glyph cell caused; `font_string_width` reports the real width and every alignment site uses it instead of `strlen*8`. Single-glyph `font_draw_char` callers (terminal grid) keep the fixed cell. Regression: `texttest` / `tools/textspacing-check.sh` |
| `drivers/window.c` | The one render-target abstraction every draw call in this kernel goes through: `window_pixel`/`window_rect`/`window_clear`, a viewport, an optional offscreen "screen band" for partial repaints, and a single redirectable supersample target (icons render into it at several times real size, then box-downsample for real anti-aliasing). v41's `window_open_scaled` opens a physical mode `s` times the logical one so `window_pixel` quietly fans out into an sxs block, the real fix for "the pixels are too big," not more vector polish on top of an 800x600 ceiling. Issue #14 ("everything super laggy"): `window_present_ticks_last`/`window_present_ticks_max`, PIT ticks a present took from the first damaging draw call of that frame to the copy finishing, read by symbol from `tools/checks/frametime-check.py`; and `window_fill_rect_phys`, a bulk solid-color rect fill that pays the viewport/back/screen_band routing once per rect instead of once per pixel, used by `window_clear`'s viewport branch and by `gui_rounded_rect_on_wallpaper`'s solid interior (kernel.c), which together cut opening Files from 44-68 ticks/present to 28-46 |
| `kernel/console.h` | Three-function VGA text-console prototypes (`putc`/`puts`/`puthex`), the header `kernel.c`'s own console implementation exposes to the rest of the kernel before the GUI exists |
| `lib/libc.c` | `memcpy`/`memset` and the other handful of freestanding primitives this kernel can't pull from a real libc (there isn't one), used everywhere byte-level copying is needed |
| `drivers/png.c` | v74 (0.66.0): minimal PNG decoder, the "prerequisite bridge" the satellite-wallpaper goal was blocked on. Scope is deliberately narrow and enforced (8-bit RGB/RGBA, and since v75 8-bit indexed with a PLTE lookup on output, because every real plain-HTTP map tile server found serves paletted PNGs; non-interlaced; anything else returns `PNG_E_UNSUPPORTED` rather than misdecoding, and an indexed image with no PLTE or an index past the palette is `PNG_E_FORMAT`), but underneath it is a real RFC 1950/1951 zlib inflater (stored, fixed-Huffman and dynamic-Huffman blocks, canonical-Huffman decode in the zlib "puff" shape, no recursion, ~300 bytes of stack tables) plus all five RFC 2083 scanline filters (None/Sub/Up/Average/Paeth). Chunk CRC-32s and the zlib Adler-32 trailer are both verified, so a corrupted byte fails loudly (`PNG_E_CRC` / `PNG_E_ZLIB`) instead of returning garbage pixels. Output is one kmalloc'd `w*h*channels` buffer in exactly `wallpaper_rgb`'s byte layout. Tested two ways: `tools/checks/png-host-check.sh` compiles the same file natively against `tools/png-host/` shims (seconds), and the in-kernel `pngtest` decodes four real PNGs cut from the wallpaper itself (three truecolor, one 40-color indexed since v75, compared by host hash since its pixels are palette lookups of a quantized crop) (`drivers/png_testdata.h`, generated by `tools/gen/gen_png_testdata.py` with every filter type cycled per row and each DEFLATE block kind asserted from the stream bytes), compares pixel-for-pixel against `wallpaper_rgb`, and prints an FNV-1a hash that `tools/checks/png-check.sh` matches against the host's PIL decode of the identical bytes |
| `drivers/jpeg.c` | v76 (0.72.0): minimal baseline JPEG decoder, part 1 of the real prerequisite the PNG bridge's own honest finding named: every plain-HTTP satellite tile source found (Google `mt0.google.com/vt/lyrs=s`, Bing `ecn.t0.tiles.virtualearth.net`) serves JPEG, not PNG; both were fetched and their SOF0 headers confirmed real baseline/8-bit/3-component/4:2:0 before committing to this scope. Deliberately narrow and enforced the same way png.c is: baseline DCT only (SOF0), 8-bit precision, 1 (grayscale) or 3 (YCbCr) components, Huffman entropy coding, 4:4:4 or 4:2:0 subsampling, a single interleaved scan; progressive/extended-sequential/lossless (SOF1-3), arithmetic coding (SOF9-15), 12-bit precision, CMYK, 4:2:2 or other odd subsampling, restart markers (any `DRI` segment) and multi-scan baseline all return `JPEG_E_UNSUPPORTED` cleanly rather than misdecoding. Underneath: real marker parsing (SOI/APPn/DQT/SOF0/DHT/DRI/SOS/EOI), canonical Huffman decode of DC/AC coefficients (JPEG's DHT already lists symbols in canonical order, no sort step needed), zigzag dequantization with an overflow-safety clamp on coefficient magnitude, a real separable fixed-point IDCT (an 8x8 scaled-cosine table baked in at compile time, no runtime floating point since this kernel builds `-mno-sse -mno-mmx` with no FPU init), YCbCr->RGB (ITU-R BT.601 integer coefficients), and nearest-neighbor chroma upsampling (not libjpeg's fancy/triangle filter, a documented, deliberate simplification). Output is one kmalloc'd `w*h*channels` buffer, RGB or grayscale, `png_decode`'s exact convention. Tested two ways, but JPEG is lossy so the two checks split the burden PNG's single check covered: `tools/checks/jpeg-host-check.sh` compiles the same file natively and compares against PIL's own decode of five real photographic JPEGs (`drivers/jpeg_testdata.h`, crops of the same wallpaper photo `png_testdata.h` uses, re-encoded at varied quality/subsampling) within a documented tolerance (max per-channel diff <= 24, mean abs diff <= 6, real lossy-decoder disagreement from the upsampling/IDCT choices above, not a bug); the in-kernel `jpegtest` decodes the identical bytes and `tools/checks/jpeg-check.sh` demands its FNV-1a hash match a *fresh* native run of the same deterministic integer-only source exactly, proving the freestanding cross-compiled build agrees bit-for-bit with itself compiled natively, plus two fault injections (no SOI marker, truncated file) |
| `drivers/serial.c` | Minimal polling COM1 (0x3F8) output, debug-only, not wired to any shell command: a live boot trace readable with `-serial stdio`, immune to whatever state a crash leaves the VGA framebuffer in. `klog()` mirrors every entry here too. The one channel that made v54/v56/v58/v59/v62/v63/v64's headless verification passes possible, a boot-time direct-call trick can dump real values to it without ever opening a display |
| `drivers/mouse.c` + `drivers/vmmouse.c` | Pointer input. `mouse.c` is the PS/2 mouse on the 8042's second port (IRQ12, 4-byte IntelliMouse packets with wheel support since v0.76.29). `vmmouse.c` (v62) probes the VMware absolute-pointer backdoor on I/O port 0x5658 at boot; where a host answers (QEMU's default pc machine, v86 in the browser) it switches the host to absolute mode and the GUI takes positions from it, PS/2 bytes still drained for phase but ignored. No backdoor (bare hardware, `-machine vmport=off`): PS/2 relative stays the only mouse |
| `kernel/gui_prompt.h` | v0.76.24: shared prompt-input helper for multi-app use. Two functions: `gui_prompt_line_input` (standard prompt at y=52, one-line text input at y=76) and `gui_prompt_line_input_with_date` (same pattern, for Calendar's date-string input with arrow-key navigation over YYYY-MM-DD fields). Used by Mail's compose, Reminders' add, Chat's message input, and Calendar's event-add flow, eliminating the per-keystroke full-redraw bug each app had before consolidating to this shared chrome/content split |
| `kernel/auth.h` | v0.77: real user accounts, header-only like `reminders.h`. A from-scratch SHA-256 (FIPS 180-4, verified against real published test vectors incl. the empty-string/"abc"/56-byte multi-block vectors in `tools/checks/auth-check.sh`), salted and iterated (`AUTH_HASH_ROUNDS`, ~0.33s measured real cost) into `USERS.TXT` (`username:saltHex:hashHex`, the same `vfs_replace_file`/`vfs_read_file` pair every other app file uses, every field bounds-checked as untrusted disk content), a constant-time hash compare, and real login/first-run-style GUI screens reusing `gui_prompt.h`'s chrome. `auth_gate()` (called from `gui_run()`) is opt-in: it only shows the login screen once at least one account exists in `USERS.TXT`, an unconfigured system (including the public v86 landing demo) boots straight to desktop with nothing to protect -- see `docs/THREAT-MODEL.md`. Creating the first account and changing your own password both live in Settings, not the boot path |
| `docs/THREAT-MODEL.md` | The real threat model `kernel/auth.h` was built against, written first: what a Joshua Tree login screen protects (the desktop, for whoever is at the keyboard on a single-machine demo) and what it explicitly doesn't (network multi-tenancy, disk encryption, ring-0/ring-3 isolation -- none of which exist in this kernel), plus why the gate is opt-in rather than on by default |
| `tools/checks/auth-flow-check.py` | The 1.0 gate's end-to-end proof of the whole account story on a real, fresh disk image, not just `auth-check.sh`'s host-compiled unit suite: Settings -> Add user really persists to `USERS.TXT` with no plaintext ever touching disk, a reboot enforces a real blocking login screen (the dock is inert behind it, esc on either field and an empty password never bypass it, a wrong password is rejected with a real measured ~1s throttle delay bracketed by `window_present_count`, never a bare sleep), Settings -> Account really rotates the password, and a second reboot proves the old password is rejected and the new one accepted. Found and fixed two real bugs while being written: (1) `kernel/auth.h`'s login-rejection message and two status lines in `kernel/kernel.c`'s `gui_launch_settings` (`"Password changed."`/`"Account created."`) were drawn into the back buffer but never `window_present()`ed before `sleep_ticks()`, so they never reached the visible screen -- `drivers/window.c`'s own header comment already documents that contract; (2) Settings' click hit-test (`settings_row_at`) read `app_cursor_x`/`app_cursor_y`, which `gui_app_mouse_tick()` only keeps live for apps opened through `gui_launch_from_dock`'s windowed wrapper -- `gui_launch_settings()` is entered straight from the Apple menu and never gets that wrapper, so every real click landed on stale (often 0,0) coordinates and silently acted on whichever row happened to already be selected, the exact gap `walldemo-regression-check.py`'s own comment had already flagged as unconfirmed. Fixed by querying `mouse_get_absolute()` directly at the click site |
| `kernel/kernel.c` | VGA text console, PS/2 scancode table, RTC clock, the shell, and a mouse-driven GUI desktop (`gui`) built on v6's graphics/font/mouse primitives. The GUI runs at 960x540 logical, scaled 2x to 1920x1080 physical; dock geometry and app layout live here too. Magnet-style window snapping (`gui_snap_zone`/`gui_snap_target`/`gui_snap_outline`, driven from `gui_run`'s title-bar drag handling): a press on a multi-window app's title bar, then a real drag past the threshold, shows a 1px outline of the target rect once the pointer enters an edge/corner zone (redrawn only on a zone change, not on every mouse move, since there is no per-window back buffer to hide a live-follow redraw behind); releasing inside a zone snaps the window's real x/y/w/h to that half/quarter/full rect, releasing outside one does a real free move to the drop position. Proven pixel-for-pixel by `tools/checks/windowsnap-check.py`, alongside `tools/checks/multiwindow-check.py` |
| `kernel/kernel.c` | VGA text console, PS/2 scancode table, RTC clock, the shell, and a mouse-driven GUI desktop (`gui`) built on v6's graphics/font/mouse primitives. The GUI runs at 960x540 logical, scaled 2x to 1920x1080 physical; dock geometry and app layout live here too. Apple menu (tree logo) contains About (memory+uptime stats), Files, Notes, Settings, Lock Screen (v1.0 security: re-engages login if accounts exist, shows brief message if not), Restart, and Shut Down. Magnet-style window snapping (`gui_snap_zone`/`gui_snap_target`/`gui_snap_outline`, driven from `gui_run`'s title-bar drag handling): a press on a multi-window app's title bar, then a real drag past the threshold, shows a 1px outline of the target rect once the pointer enters an edge/corner zone (redrawn only on a zone change, not on every mouse move, since there is no per-window back buffer to hide a live-follow redraw behind); releasing inside a zone snaps the window's real x/y/w/h to that half/quarter/full rect, releasing outside one does a real free move to the drop position. Proven pixel-for-pixel by `tools/checks/windowsnap-check.py`, alongside `tools/checks/multiwindow-check.py` |
| `tools/png-host/` + `tools/jpeg-host/` | Host-native test harnesses, not kernel code: each pairs a `main.c` fixture with tiny `kheap.h`/`libc.h` shims (a plain `malloc`-backed `kmalloc`, a real `memcpy`/`memset`) so `drivers/png.c`/`drivers/jpeg.c` compile and run unmodified under the host's own clang, no QEMU, no freestanding flags. `tools/checks/png-host-check.sh`/`jpeg-host-check.sh` run these as the fast inner loop while iterating on either decoder; `tools/checks/png-check.sh`/`jpeg-check.sh` separately prove the identical source also works inside the real freestanding in-kernel build, the two together are what "tested two ways" in this decoders' own rows above means |
| `tools/png-host/wallsat_check.c` | One-off host check for the baked satellite wallpaper: decodes `kernel/wall_sat.h` through the real `drivers/png.c` and confirms it comes back 960x540 RGB, the layout the wallpaper code expects. Not part of the regression suite. |
| `tools/fuzz-host/fuzz_parsers.c` + `tools/parsers-host/` | Host ASan/UBSan fuzzer for the three parsers that read untrusted bytes off the network or a mounted disk: `drivers/http.c` (status-line/header parsing), `drivers/json.c` (the key-value extractor weather/chat use), and `drivers/fat.c` (FAT16 boot sector, directory entries, cluster chains), run by `tools/checks/parser-fuzz-check.sh`. Same shape as `tools/fuzz-host/fuzz_decoders.c`: seed samples truncated at every length (or ~200 spaced lengths for the FAT image), ~2000 fixed-seed byte mutations per sample, and handcrafted nasties, all under a per-input `alarm()` so a hang fails loudly instead of hanging CI. `drivers/net.c` is not linked in (a real NIC/TCP state machine, nothing to host-fuzz); `http.c`'s two calls into it (`dns_resolve`, `tcp_get_timeout`) are stubbed here, `tcp_get_timeout` handing back the fuzzed bytes as the "server reply" so `http_get`/`http_post`'s real header parsing is what actually runs. `fat.c` is fuzzed against a mutated in-memory disk image served through the real `drivers/blockdev.c` registration API, the same one `drivers/ramdisk.c` uses, bounds-checked against the buffer's real size so a corrupted boot sector chasing an lba off into nowhere is rejected instead of ever being an out-of-bounds access in the harness itself. `tools/parsers-host/kheap.h`/`libc.h` are the same malloc/string.h shims `tools/png-host/`/`tools/jpeg-host/` already use. Found one real bug: a boot sector's `total_sectors_16`/`total_sectors_32` fields are read with no relation checked against `fat_size_sectors` (the field that actually sizes the on-disk FAT table), so a hostile or merely corrupted image (an interrupted format leaves exactly this) could make `total_clusters` come out near 4.3 billion; `alloc_cluster`'s free-cluster search (`fat.c`) has no other bound, so any write against such a disk hung for the full search, a real denial of service. Fixed in `fat_mount()` by clamping `total_clusters` to what `fat_size_sectors` can actually index, the same "cap it at the one place that computes it" fix `MAX_DIR_CLUSTER_HOPS` already applied to directory-chain walks; the triggering image is now a permanent handcrafted nasty in `fuzz_parsers.c` itself (`fat_alloc_cluster_dos`), not just a one-off saved `.bin`, so it reruns every check like every other nasty here |
| `tools/fuzz-host/fuzz_decoders.c` | Deterministic fuzz driver for `drivers/png.c` and `drivers/jpeg.c`, run by `tools/checks/decoder-fuzz-check.sh` under address and undefined-behavior sanitizers. Feeds each decoder truncations of valid samples at every length, roughly 2000 deterministic single/multi-byte mutations per sample, and handcrafted corner cases, with per-input timeouts to catch hangs. Validates robustness: crashes, sanitizer reports and timeouts fail loudly instead of silently corrupting the kernel. |
| `docs/BLUEPRINT.md` | The structural plan for after 1.0: research against SerenityOS, ToaruOS, Haiku, Redox, Essence, xv6 and KolibriOS/MenuetOS, the target architecture (apps as ring-3 processes talking to a window server), and a phased, PR-sized plan from finishing the `kernel.c` app extraction through porting every app off ring 0 |
| `docs/SYSCALL-ABI.md` | The v1 syscall contract, and the real gate on 1.0.0: semver's 1.0.0 claims an interface an outside consumer can rely on, and until a ring-3 program is written against a fixed one there is nothing for a MAJOR version to protect. Writes down the existing `int 0x80` convention (eax = number, ebx/ecx/edx = args, negative errno, pointers checked against the page tables before any kernel read), the v1 call set using Linux i386's own numbers, the limits that are part of it (255-byte copies, 63-byte paths, 8KB per open file, five open files per task, the 0xC0500000 load address), which calls are shipped, what the contract deliberately does NOT cover (no `mmap`, no `fork`, no signals, nothing about the GUI), and what counts as a breaking change from here |
| `tools/watch.sh` | Live dev loop: keeps a real QEMU window open and reboots it whenever the BUILT `kernel.elf` hash actually changes, so a background agent touching a file or a rebuild that changes nothing never flashes the window. A failed build leaves the last good VM running rather than killing it. Recopies `landing/v86/kernel.elf` on every real change. Costs nothing but this Mac's own CPU |
| `tools/qa-demo.sh` + `tools/qa_demo_drive.py` | Headless dogfood run, recorded to video. Boots with no display, drives the desktop over QMP with absolute-pointer clicks and real keystrokes, opens every dock app, types real demo content into Notes/Reminders/Terminal/Chat, scrolls the Apps folder, and screendumps a frame at every step including idle holds (a stray repaint shows up exactly when nothing should be moving), then ffmpeg turns the frames into an mp4 and a gif. Not a pass/fail test, `tools/checks/` holds the assertions: this is the dogfood pass to run before every MINOR release |
| `art/icons/` + `tools/gen/gen_icon_art.py` + `kernel/icon_art.h` | Authored dock/app icon artwork, replacing runtime primitive assembly for the icons that have it. Each icon is one hand-authored 128x128 SVG in `art/icons/` carrying the things a primitive pass structurally cannot give a tile: per-pixel interior shading, a real blurred glyph drop shadow, and a rim light that fades along the squircle's own curve. `tools/gen/gen_icon_art.py` rasterizes them on the host with `rsvg-convert` into `kernel/icon_art.h` as straight RGBA, the same host-side-generator pattern `gen_vga_font.py` and `gen_png_testdata.py` already use. The kernel never decodes anything: `gui_render_icon_cached` area-averages the 128x128 source down to the physical size it is drawing at (74 dock, 92 magnified, 120 Apps grid, all downsamples, which is why one authored size beats a ladder of pre-rendered ones) and composites it over the surface colour, producing exactly the opaque tile the existing icon cache already expected. Colour is averaged weighted by alpha so the transparent border cannot drag edge pixels into a fringe; integer only, no FPU. Icons without artwork keep the old `gui_icon_*` primitive path unchanged, so the conversion is incremental. Artwork is stored as the PNG bytes `rsvg-convert` produced and `png_decode`d on an icon-cache miss, not as decoded RGBA. That is a real constraint, not a preference: 24 artworks of 128x128 RGBA is 1.5MB, which collides with the ring-3 program window `boot/linker.ld` pins at `0xC0500000` and `docs/SYSCALL-ABI.md` names as part of the published v1 contract. The same 24 as PNG are 141KB (`gen_icon_art.py --budget`), leaving ~695KB between `.bss` and that window, and they need no new decoder because `drivers/png.c` has shipped since v74 and `rsvg` emits exactly its supported subset (the generator re-checks every file's IHDR rather than trusting that). Proven equivalent: the dock and the whole Apps grid are pixel-identical between the RGBA-stored and PNG-stored builds, zero differing pixels. Icons with runtime state get a second artwork through `ICON_ART_VARIANT`, selected by the `variant` the icon cache already keys on, so converting Trash did not throw away its empty/full indicator |
| `tools/gen/gen_wall_sat.py` + `kernel/wall_sat.h` | A REAL, once-captured satellite photograph, baked in for the v86 browser demo's automatic no-network wallpaper fallback, the same "host-side generator produces a committed C header" pattern `gen_wallpaper.sh` (the tree photo) and `gen_icon_art.py` already use. Direct owner request, Sep 2026: the demo should show a satellite look, not the tree photo, but v86 genuinely has no path to a LIVE fetch completing (`wall_apply`'s `font_is_fallback()` branch), so the only honest fix is baking a real capture in at build time. `gen_wall_sat.py` fetches the SAME real tiles `wall_fetch()` fetches for `WALL_SAT` (`mt0.google.com/vt/lyrs=s`, plain HTTP, real baseline JPEG) at the same zoom/tile-grid/crop math, for Langley, BC (this repo's own real-world reference point, since `wall_fetch()` itself has no compiled-in default location). Stored as an 8-bit indexed (32-color, PLTE) PNG, exactly the shape `drivers/png.c` has decoded since v75: quantizing the 1,555,200-byte raw 960x540x3 capture drops it to 276,607 bytes, a 5.6x saving, small enough to fit inside the ~638KB of headroom the kernel image had left under `boot/linker.ld`'s `0xC0500000` ring-3 program window ceiling (measured before/after via the ELF section headers; ~351KB remains after this addition). `wall_sat_init()` in `kernel/kernel.c` `png_decode()`s it once, lazily, on first need and keeps the decoded 960x540x3 buffer for the session, the same permanent-buffer lifetime `wall_dark_fallback_init()` already uses. `wall_apply()` only reaches for it when a map theme is selected, no `wall_map` has loaded, AND `font_is_fallback()` is true (the real v86 signal); native QEMU with real networking is untouched (dark fallback while a fetch is pending, then the real live map). The tree photo (`wallpaper_rgb`) stays fully reachable as an explicit Settings pick (`WALL_PHOTO`) on every platform, this only changes the automatic no-network default. `tools/checks/wallboot-v86-check.mjs` proves it headlessly against the real v86/libv86.js emulator, reading `window.__jt.serial` for the `wallsrc=` marker `wall_apply()` logs at its one real choke point |
| `tools/checks/dockshadow-check.py` | Proves the dock icon contact shadow falls off smoothly rather than in bands. Boots headless, waits for a real `window_present`, and measures the shadow's horizontal luminance profile on three rows under every one of the ten dock icons, asserting both the largest adjacent-pixel jump and the number of distinct luminances: a real gradient moves a little at a time and visits many values, a banded one sits on a few plateaus and steps between them. Written against a real defect, `gui_draw_icon_shadow`'s `int sdx = dx * ry / rx`, integer division that collapsed 75 columns into 17 values and made the shadow 9 flat plateaus (max step 20-21, 8-9 distinct; after the fix 5 and 30-34) |
| `tools/checks/iconart-check.py` | Proves the authored artwork is what the dock actually drew, not just what is linked in. Boots headless, `pmemsave`s the real 1920x1080 framebuffer, and asserts every dock tile's background, at two glyph-free bands, matches the tile gradient its own SVG states (`Tile #RRGGBB -> #RRGGBB`, written by `restyle_icons.py`) within 8 per channel; the primitive fallback and any stale art paint other hues. Also runs `gen_icon_art.py --check` first so a stale generated header can't make the pixel assertions prove something about the wrong build. Discriminating: the pre-Big-Sur build misses by 118-186 on every slot, this one by 0.3 at most |
| `tools/checks/html-host-check.sh` | Host harness for `drivers/html.c`: named entities become their closest ASCII (`&middot;` to `-`, curly quotes to straight), an unknown entity is dropped whole instead of printed raw, and a bare `&` survives. Written after Plan's page showed a literal `&middot;`. |
| `tools/checks/input-bounds-check.sh` | Host harness (ASan/UBSan) left behind by the Sep 2026 security QA pass over every fixed-size text buffer in `kernel/kernel.c`/`kernel/*.h`. Compiles `drivers/json.c` for real (the network-JSON parser weather/geo/chat responses go through) and feeds `json_extract_string`/`json_extract_number_text`/`json_escape` `maxlen` 0 and 1 plus values far longer than the output buffer, proving the `maxlen - 1`/`maxlen - 2` unsigned-underflow guard that pass added actually holds and every truncation still lands a real NUL inside bounds. Also exercises `kernel/auth.h`'s `auth_const_time_eq` (copied into the test driver, see the script's own comment for why) over 256-byte buffers with a last-byte-only and a first-byte-only mismatch, proving it doesn't short-circuit. Registered in `tools/checks/ci-suite.sh` next to `decoder-fuzz-check.sh` |
| `tools/checks/iconlight-check.py` | Pins the Big Sur lighting on all eleven dock tiles from a headless `pmemsave`: top band lighter than the bottom band by 6-45 luminance (a few percent, not the glossy 90-99), a highlight on the top row, no dark bottom outline (bottom band to last row drops at most 6; glossy tiles dropped 17-28), all measured on glyph-free background rows. Discriminating: every slot fails on the glossy build, every slot passes on this one |
| `tools/checks/version-bump-check.sh` | Runs in CI's check-refs job on every PR: a PR that changes anything but Markdown must raise VERSION above main's, so every merge cuts a release through release.yml. Prose-only PRs are exempt. |
| `tools/checks/dockband-check.py` | Pins the shadow under the dock tray to the real photo from a headless `pmemsave`: along each of the 16 rows under the tray no single colour may cover more than half the row. The old one-colour-per-row shadow covered 100% and drew a striped bar; the per-pixel one covers under 15%. |
| `tools/checks/iconsync-check.sh` | The brand mark lives in three real places that drift: `landing/icon.svg` (the source), `icon.svg` at the repo root (what README renders), and the app bundle's generated `icon.icns`. A real rebuild updated only the first, so GitHub kept rendering the old art while the app and landing page showed the new one. Same class `versionsync-check.sh` guards for `VERSION` and `landing/version.txt` |
| `tools/checks/appsfolder-layout-check.py` | Proves the Apps folder's 1.0 QA pass against real pixels: no dead black band under the title bar (`gui_draw_wallpaper_rows_sway_ex`'s `GUI_MENUBAR_H` clamp, and `gui_wallpaper_row`'s vertical scale, both used the desktop's reserved-menubar assumption inside a windowed app's own clipped viewport, which has no menu bar of its own -- both now gated on `gui_app_windowed`), no icon-toned pixels between the glass panel's bottom edge and the window's own bottom edge, and no ghost icons left over in a scrolled-to-the-end row. The row spill and the ghost icons were one root cause, not two: `gui_apps_draw_grid`'s visibility check compared `row * cell_h` against the panel's pixel height (375) instead of the row count it actually fits (`APPS_VIS_ROWS`, already named two lines above it), so a 4th row 108px too tall for the panel slipped through, got cut by the window's bottom edge, and (being outside the 375px rect every later panel-only repaint clears) left stale pixels behind once a scroll changed which apps that row should hold. Boots headless, opens the Apps folder from the dock (QMP absolute pointer, same pattern as `launchpad-click-check.py`), `pmemsave`s the framebuffer on open and again after real QMP wheel-down events scroll to the end. Discriminating: reverting the row bound or the `gui_app_windowed` gating each fail this independently; both reverted together reproduces the original screenshot |
| `tools/checks/qa-gallery.py` | Headless QA gallery: opens every app (all 26, accessible from the Apps folder grid or dock), saves a full-resolution 1920x1080 screenshot of each, closes it, and fails on any crash. The main session visually reviews every PNG, so the gallery is the product. Same QMP absolute-pointer + pmemsave + grid-navigation pattern as `activity-check.py`/`appclose-check.py`: opens the Apps folder (dock slot 0), navigates by arrow keys (row = i // 5, col = i % 5), presses Enter to launch, dumps the framebuffer with `pmemsave`, closes via pointer or esc, repeats. Outputs `<outdir>/NN-name.png` per app (default `/tmp/jt-gallery`, overridable by argv[1]). Fails if any app never opened a window or if the serial log contains panic/exception strings. All 26 apps opened cleanly with no crashes detected |
| `tools/checks/feature-drive.py` | Every app's main action, headless: opens each app, performs its primary action (edit, add, navigate, type, vote, etc.), polls for real signals (pixel changes via PIL ImageChops), saves a before/after screenshot, and closes it. Exercises each feature once, reporting which actions changed the screen. Same QMP/pmemsave/grid-navigation pattern as `qa-gallery.py`, extended with action-specific key/text sequences. Fails on any crash or app that never opens |
| `tools/checks/soak-check.py` | Headless soak test: opens and closes every app N times in one boot, proves nothing leaked. Reuses boot, QMP, navigation and close code from `qa-gallery.py`. Tracks `pmm_free_frames_last` and `task_used_last` kernel variables (updated by `window_present()` in `drivers/window.c` via `pmm_free_frames()` and `task_used(i)`) before and after pass 1 and pass 20 to detect leaks via QMP human-monitor-command `xp` symbol reads. Takes argv[1] for pass count (default 20) and argv[2] for optional comma-separated app indices to bisect. Fails if free frames dropped by more than 64 (256 KB), task slots grew, or the serial log contains panic/exception strings |
| `tools/checks/frametime-check.py` | Issue #14 ("everything super laggy"): the permanent frame-time gate. Reads `drivers/window.c`'s `window_present_ticks_last`/`window_present_ticks_max` by symbol (`nm` + QMP `xp`, same shape `editor_qa.py`'s `Machine` class already established) across three scenarios -- idle desktop, the pointer hopping across the dock (`dockhover-check.py`'s own move sequence), and opening Files -- polling for the real-time max each scenario hits rather than one sample, since an ambient redraw can overwrite the symbol between polls. FAILs if any scenario, or the session's single worst present, is over its budget. Root cause found with this instrument: `window_clear`'s viewport branch and `gui_rounded_rect_on_wallpaper`'s solid interior (kernel.c, used by every window's chrome, the dock tray, and every flyout panel) both walked every pixel through the generic per-pixel damage-tracked path even though most of that area is flat, unblended color; fixed with `window_fill_rect_phys` (`drivers/window.c`), a bulk solid-rect fill that pays the routing once per rect instead of once per pixel. Cut opening Files from 44-68 ticks/present (440-680ms) to 28-46 (280-460ms) |
| `tools/gen/inject-landing-facts.py` | Computes the landing page's fact row (app count, regression check count, real kernel source lines, latest version) straight from the source and rewrites the `<span data-fact="...">` marks in `landing/index.html` in place, the same job `inject-landing-headline.sh` already does for the roadmap headline. App count reads `kernel/kernel.c`'s `GUI_LABELS` minus the Apps folder tile and Trash; check count reads the `once`/`retry` lines in `tools/checks/ci-suite.sh`'s manifest; line count reuses `tools/gen/progress.sh`'s real-code filter (same excluded dirs/generated headers/app-port prefix) summed over the files on disk now rather than replayed from git history, so it can differ from `progress.svg`'s own number by a handful of lines on a rename-heavy history; version reads `VERSION` |
| `tools/checks/landing-facts-check.py` | Drift guard for the fact row above: fails if any `data-fact` span in `landing/index.html` disagrees with what `inject-landing-facts.py` computes right now, the same class of check `versionsync-check.sh` runs for `VERSION`/`landing/version.txt` and `landing-headline-check.sh` runs for the roadmap headline |
| `tools/checks/lockscreen-check.py` | Proves Lock Screen (the Apple menu item, `kernel/kernel.c`'s `gui_lock_screen`) actually works, not just that it doesn't crash: with no accounts, clicking it shows a brief message and returns to the desktop intact; with an account, it forces `auth_logged_in` back to 0, the real login screen re-blocks (proven the same way `auth-flow-check.py` proves it -- a click on the Notes dock icon leaves `editor_loaded` untouched), Esc and a wrong password cannot bypass it, and the right password brings the desktop back interactive. Reuses `auth-flow-check.py`'s own `Machine` class and Settings-navigation helpers (copied in, since that file runs its whole check at import time and isn't import-safe) to create its test account for real through Settings rather than hand-writing `USERS.TXT`, boots three times (account creation, then a real reboot for the login/lock/unlock story), and cross-checks both the framebuffer dock pixel and the serial "auth: locked" marker. Found and fixed a real rendering bug while being written: the no-account message box drew a full-size fill rect then a second full-size rect in the border color directly on top of it, burying the message text on an now-identical grey background |

## Apps

Two different shapes of app live in this kernel, both dock-mounted or
Apps-folder-mounted, both counted in `GUI_APP_COUNT` (22 real apps, plus
the Apps-folder tile and Trash, `kernel/kernel.c`).

**Five built-in apps, each its own header, each VFS-backed.** Same
persistence pattern every time: a fixed-size static array in RAM, one plain
text file on the real FAT disk (`vfs_replace_file`/`vfs_read_file`), manual
line parsing (no `sscanf`, no libc), write-through on every mutation, no
separate Save step.

| App | File | Format |
|---|---|---|
| Notes | `kernel/editor.h` | `NOTES.TXT`, a single 4095-byte buffer. The one app with real typography: `editor_fonts.h`'s embedded DejaVu Sans/Serif/Mono glyph table, F1/F2/F3 pickers for family/size(4)/weight, a real caret with vertical up/down that tracks column across lines |
| Reminders | `kernel/reminders.h` (v52 / 0.52.0) | `REMINDERS.TXT`, one line per item: `0`/`1` done flag, a space, the text. Up/down/select, `a` adds, `d` deletes, space toggles done |
| Calendar | `kernel/calendar.h` (v54 / 0.54.0 grid, v55 / 0.55.0 events) | No file for the grid itself, today's date comes straight from the RTC's CMOS BCD registers each time; day-of-week is Zeller's congruence, swept against libc's own `timegm` for every day 1900-2099 (`tools/check-calendar.sh`, 0 mismatches) before shipping. Events: `EVENTS.TXT`, one `YYYY-MM-DD\|text` line per day, one event per date (a second add on the same day overwrites, doesn't append) |
| Mail | `kernel/mail.h` (v59 / 0.58.0) | `MAIL.TXT`, `\|`-delimited `from\|subject\|body\|read`. Two starter messages compiled in so the inbox isn't blank before a real `MAIL.TXT` exists, which always wins once it does. No SMTP/IMAP client, deliberately: a local mail-shaped app, the same relationship Reminders has to a real to-do sync service |
| Contacts | `kernel/contacts.h` (v70 / 0.64.0) | `CONTACTS.TXT`, same VFS write-through shape as the other three: a fixed-size static array (name/phone/email), view/add/delete. No CardDAV/sync backend, deliberately, same offline-only call Mail already made |
| Chat | `kernel/chat.h` (v85 / 0.70.0) | `CHAT.TXT`, `role\|content` per line, an 8-message bounded ring (oldest drops once full). Real scrollback GUI (`n` sends, `c` clears, esc closes) plus the shell `chat <message>` command, both calling the same `chat_send` against Ollama's `/api/chat` (real conversation history, not v10's one-shot `/api/generate`). Model/host/port read from Settings-persisted `llm_model`/`llm_host`/`llm_port` (`kernel.c`'s `settings_load`/`settings_save`), not hardcoded; the Settings model row cycles only real installed models (`llama3.1:8b`, `qwen3:8b`), never free text |

**Calculator** (`kernel/calculator.h`, v70 / 0.64.0) doesn't fit the VFS-backed table above, it has no persistence at all on purpose: a recursive-descent parser over `+ - * / ()` and numbers, one-line input evaluated on enter. Ported from numen's real calculator parser (the v6 roadmap note that first flagged it as portable, pure logic, no network dependency).

**Stocks** (`kernel/stocks.h`) is the basic native one, shaped like macOS Stocks: a scrolling watchlist sidebar (sparkline, price, colored change pill) beside a detail pane with 1D/1W/1M/3M/1Y range tabs, a line chart and an Open/High/Low/Mkt Cap/P/E grid. Baked-in demo data (all plain-HTTP quote sources force HTTPS, no TLS here); chart history is a deterministic seeded walk pinned to the real baked-in price. Its drawing helpers (`stx_*`) are reused by Epiphany.

**Epiphany** (`kernel/epiphany.h`) is our own, richer take, a deliberately separate app from Stocks: the offline slice of github.com/nulljosh/epiphany. Tabs: Markets (editable watchlist, `a` add / `d` remove, plus crypto, commodities, fear/greed), Portfolio (holdings valued off live-ticking prices, P/L, allocation bar, +/- shares), Simulator (ticking random-walk market you trade against), Situation (macro pulse, brief). The real app's map, People graph and accounts/billing/sync need HTTPS and cannot run here. Dock/Apps icon 22.

**Search** (`kernel/search.h`, v0.86.0), Apps-folder-only like Contacts/
Calculator/Stocks, no persistence of its own since it mirrors the real
filesystem rather than owning a file. Scoped honestly against what the VFS
actually supports: `vfs_list` (`drivers/vfs.h`) is a real driver-level
directory listing, the same call Files (`gui_draw_files_content`) already
uses, but there is no recursive whole-disk index anywhere in this kernel,
kernel or ring-3, so Search lists and filters the current directory of
whichever backend `fsuse` has active, exactly Files' own scope, not a
fabricated global index. Real-time substring filter, case-insensitive, as
you type (`gui_prompt.h`'s chrome/content split, so filtering never redraws
the whole window). Enter or a click on a match does something real: a
directory result `vfs_chdir`s into it and reloads a fresh real listing
(the same primitive the shell's own `cd` uses); a file result shows its
real bytes via `vfs_read_file` + `render_wrapped_text`, the same pair the
shell's own `cat` command and `gui_launch_html` already use.

**Fleet icons** (`tools/gen/import_fleet_icons.py`, v0.91.0), copies each project's real `icon.svg` into `art/icons/`, clipped to the shared dock tile shape, so the dock shows the icon the app actually ships with. Run it, then `gen_icon_art.py`.

**Quotes** (`kernel/quotes.h`, v0.89.0), Quotestreak native: a film line, four titles, 1-4 or a click answers, streak and best kept for the session. Replaces the one-line HTML card the dock tile used to open.

**Toroid** (`kernel/toroid.h`, v0.88.0), Conway's Game of Life on a torus, native: both edges wrap, space pauses, r reseeds, c clears, a click toggles a cell. Replaces the one-line HTML card the dock tile used to open.

**Bookrank** (`kernel/bookrank.h`, v0.93.0), curated non-fiction rankings, native: left side shows ranked list (1-10) with selected row highlighted, up/down or click to select; right side shows title, author and 2-3 sentence summary wrapped to fit. Replaces the one-line HTML card the dock tile used to open.

**Curbfind** (`kernel/curbfind.h`), Craigslist deal rankings for Vancouver, native: left side shows ranked list (1-10 sample listings) sorted by deal score descending, selected row highlighted, price right-aligned in each row; up/down or click to select; right side shows listing title, price and neighbourhood, a 10-segment bar visualizing the deal score, and a brief reason why it is a good deal. Sample listings compiled in, labeled not live.

**Lexly** (`kernel/lexly.h`, v0.94.0), Spanish vocabulary drill: a word in Spanish, four English choices, 1-4 or a click answers, streak and best kept for the session. Replaces the one-line HTML card the dock tile used to open.

**Fieldbook** (`kernel/fieldbook.h`, v0.95.0), every field of science and math explained plainly, native: left side shows ranked list (1-12) with selected row highlighted, up/down or click to select; right side shows field name, studies line, and 2-3 sentence explanation wrapped to fit. Replaces the one-line HTML card the dock tile used to open.

**Plan** (`kernel/plan.h`), Joshua's ten-year education and career roadmap, native: left side shows milestone timeline with selected row highlighted, up/down or click to select; right side shows "The next ten years" heading and detailed plan wrapped to fit. Same structure and interaction as Fieldbook. Replaces the one-line HTML card the dock tile used to open.

**Sparkjar** (`kernel/sparkjar.h`), a jar of ideas people post and vote on, native: left side shows list (1-10 ideas) sorted by votes descending, selected row highlighted, vote count displayed right-aligned in each row; up/down or click to select; u key upvotes the selected idea, re-sorts, and keeps it selected; right side shows idea name, one-line pitch, and 3-step build plan wrapped to fit. Vote counts are session-only. Replaces the one-line HTML card the dock tile used to open.

**Homeqi** (`kernel/homeqi.h`), Feng shui home assessment condensed from Eva Wong's *Good Fengshui*, native: eight yes/no questions about your home (front door, natural light, bed position, clutter, water, power lines, neighborhood, windows), asked one at a time with 1/2 key choices. After each answer shows a one-line reasoning line and "any key for next". After all eight, displays final score (0-8) and a verdict message (0-3: work to do; 4-6: pleasant; 7-8: excellent). Score accumulated through the assessment, restarts on the results screen. Replaces the one-line HTML card the dock tile used to open.

**Sparkjar** (`kernel/sparkjar.h`), a jar of ideas people post and vote on, native: left side shows list (1-10 ideas) sorted by votes descending, selected row highlighted, vote count displayed right-aligned in each row; up/down or click to select; u key upvotes the selected idea, re-sorts, and keeps it selected; right side shows idea name, one-line pitch, and 3-step build plan wrapped to fit. Vote counts are session-only. Replaces the one-line HTML card the dock tile used to open.

**Fieldbook** (`kernel/fieldbook.h`), every field of science and math explained plainly, native: left side shows ranked list (1-12) with selected row highlighted, up/down or click to select; right side shows field name, studies line, and 2-3 sentence explanation wrapped to fit. Replaces the one-line HTML card the dock tile used to open.

**Homeqi** (`kernel/homeqi.h`), Feng shui home assessment condensed from Eva Wong's *Good Fengshui*, native: eight yes/no questions about your home (front door, natural light, bed position, clutter, water, power lines, neighborhood, windows), asked one at a time with 1/2 key choices. After each answer shows a one-line reasoning line and "any key for next". After all eight, displays final score (0-8) and a verdict message (0-3: work to do; 4-6: pleasant; 7-8: excellent). Score accumulated through the assessment, restarts on the results screen. Replaces the one-line HTML card the dock tile used to open.

**Sparkjar** (`kernel/sparkjar.h`), a jar of ideas people post and vote on, native: left side shows list (1-10 ideas) sorted by votes descending, selected row highlighted, vote count displayed right-aligned in each row; up/down or click to select; u key upvotes the selected idea, re-sorts, and keeps it selected; right side shows idea name, one-line pitch, and 3-step build plan wrapped to fit. Vote counts are session-only. Replaces the one-line HTML card the dock tile used to open.

**Fieldbook** (`kernel/fieldbook.h`), every field of science and math explained plainly, native: left side shows ranked list (1-12) with selected row highlighted, up/down or click to select; right side shows field name, studies line, and 2-3 sentence explanation wrapped to fit. Replaces the one-line HTML card the dock tile used to open.

**Homeqi** (`kernel/homeqi.h`), Feng shui home assessment condensed from Eva Wong's *Good Fengshui*, native: eight yes/no questions about your home (front door, natural light, bed position, clutter, water, power lines, neighborhood, windows), asked one at a time with 1/2 key choices. After each answer shows a one-line reasoning line and "any key for next". After all eight, displays final score (0-8) and a verdict message (0-3: work to do; 4-6: pleasant; 7-8: excellent). Score accumulated through the assessment, restarts on the results screen. Replaces the one-line HTML card the dock tile used to open.

**Sparkjar** (`kernel/sparkjar.h`), a jar of ideas people post and vote on, native: left side shows list (1-10 ideas) sorted by votes descending, selected row highlighted, vote count displayed right-aligned in each row; up/down or click to select; u key upvotes the selected idea, re-sorts, and keeps it selected; right side shows idea name, one-line pitch, and 3-step build plan wrapped to fit. Vote counts are session-only. Replaces the one-line HTML card the dock tile used to open.

**Portfolio** (`kernel/portfolio.h`, v0.87.0), Apps-folder-only like
Search/Contacts/Calculator/Stocks. A static, compiled-in catalog of the
whole heyitsmejosh.com fleet (35 apps across Life/Read/Make/Play/Dev),
each with a one-line description and its `<name>.heyitsmejosh.com` URL
where one exists. Same fixed-size-table shape and `gui_prompt.h`
chrome/content split as Search, plus a scrollable, keyboard/mouse-driven
list (`pf_scroll`, up/down and the mouse wheel, same row-clamping shape
`gui_apps_draw_grid`'s Apps folder scroll already uses). This kernel has
no general "open any URL live" hook: the fleet apps already ported in as
`gui_launch_html` are baked static snapshots, not a browser, and Search's
own scope is the local filesystem, not the network. So selecting an
entry shows its URL on a detail line rather than trying to open it. Apps
folder and Trash moved to 24/25 to make room.

**Activity** (`kernel/activity.h`, v0.88.0), Apps-folder-only like Search
(icon 24; Apps folder/Trash at 25/26, after Portfolio took 23), a real Activity Monitor-shaped
window over the exact primitives the shell's own `ps`/`kill`/`mem`
commands already call (`task_used`/`task_max`/`task_kill`/`task_current`,
`kernel/task.c`; `pmm_free_frames`/`pmm_total_frames`, `kernel/pmm.c`),
never reimplemented. Shows all `TASK_SLOTS` (6) real scheduler slots (pid,
a synthesized name since `struct task` has no name field -- "Shell/GUI"
for slot 0, "Task N" otherwise -- and state: running/free), real free/
total memory and uptime, refreshing about once a second off the PIT tick
independent of keyboard/mouse input (its own inlined event loop, not the
shared blocking `get_key_or_click`, since it's the one app here that has
to redraw on a timer with nobody touching it). Up/down selects a row;
`k` or the Kill button calls `task_kill` on the selection, refusing (with
a real on-screen message) to kill the current task, the kernel/GUI's own
slot. `spawntest` (a shell command added alongside it) spawns a real,
persistent task for `tools/checks/activity-check.py` to select and kill.

**Eleven apps ported natively from the fleet, thin ports on purpose.**
`tools/gen/gen_app.sh` turns a sibling repo's real single-file static build
(`~/Documents/Code/<app>/web/index.html`, this user's documented
sibling-checkout convention) into a plain C byte array,
`drivers/app_<name>.h` (`app_<name>_html`/`app_<name>_len`), no bundler, no
multi-file apps, real content only. Every array is reachable two ways:
`gui_launch_html` runs it through `html.c`'s `html_to_text` and renders the
real extracted copy read-only in the desktop; `serve_app` (the `serveapp
<name>` shell command) serves the exact original bytes over `net.c`'s
`tcp_serve_once`, verified for real over the actual network per v9/v35, not
assumed from a file check: a genuine `curl` through QEMU's `hostfwd` against
`serveapp keyrate`/`serveapp bookrank` came back the real `<title>`/meta
description, matching the actual repo content. v35 screened every candidate
by that same `html_to_text` transform before porting it, correctly skipping
apps whose real content only exists after client-side JS rendering this
kernel can't run (epiphany, healstack, blockframe, numen, curvely, roost,
monocode).

- **Keyrate** (`drivers/app_keyrate.h`): the one exception to "thin port": `gui_launch_html` originally just rendered the ported site's own marketing copy read-only, which meant the first keystroke anyone made to actually type closed the app (`gui_wait_close`'s "any key closes" contract). Fixed with a real native typing test in `kernel.c` itself: word list, a tiny LCG seeded from `irq.c`'s real `ticks()` (no `rand()`/no libc), a live input loop. `app_keyrate.h`'s ported HTML still exists, served only, not rendered in the GUI
- **Sparkjar** (`drivers/app_sparkjar.h`): idea forum
- **Homeqi** (`drivers/app_homeqi.h`): feng shui home-assessment tool
- **Weather** (`drivers/app_weather.h`, served copy only): the dock's own Weather app is native `kernel.c` logic against a live Open-Meteo fetch (temperature + WMO condition code, `weather_fetch`) for this machine's real location (v71 / 0.65.0: `geo_fetch` asks ip-api.com over plain HTTP once per boot, replacing a hardcoded downtown-Vancouver lat/lon; `tools/geo-check.sh` proves it headlessly against the host's own answer), not this file; v75 / 0.67.0: the same lat/lon drives the map wallpaper (`wall_fetch`, twelve OpenTopoMap tiles centered on the town, `tools/wallpaper-check.py`); `app_weather.h` is the ported static site, reachable only via `serveapp weather`

## Why things are ordered this way

Each subsystem depends on the ones above it: paging needs `pmm.c`'s frames,
the heap needs paging to be sane, tasks need the heap for their stacks, `fat.c`
needs `ata.c`'s sectors, `exec.c` needs the VFS's files. Bringing them up
out of order is the fastest way to a silent, hard-to-diagnose bug.

## What's here now that used to be deferred

Higher-half kernel (v2) shipped: see the Boot sequence section above.
`paging.c`/`pmm.c` are the two files that needed physical-vs-virtual care;
`rtl8139.c` needed the same care for a different reason, the NIC does raw
physical-memory DMA and doesn't know what a page table is.

Ring-3 user mode (v3) shipped: `gdt.c` adds ring-3 code/data segments and a
TSS, `paging.c`'s `paging_set_user()` marks specific pages user-accessible
while everything else stays supervisor-only, and `ring3.c`/`ring3_asm.S`
copy a hand-written payload onto a user page and run it at CPL 3. As of
v64 that payload is a real scheduled task (`task_create_user`) with a
real `int 0x80` gate to call: `ring3test` writes a line through
`SYS_WRITE`, stores the kernel's return value in a marker from ring 3, and
ends with `SYS_EXIT`; `ring3test fault` keeps the v3 privileged-instruction
payload and is reaped by `idt.c`'s ring-3 exception path instead of halting
the machine; `ring3test spin` proves preemption and `task_kill` on a ring-3
task. All three come back to the shell. Verified by independent
QEMU-monitor reads of the marker plus the kernel's own correctly-named
reports, not just "didn't crash"; see `docs/roadmap.md`'s v3 and v64 entries.

## Verification

`check.sh` is the only automated check: it boots the kernel in QEMU and
asserts the startup banner reached VGA memory. That catches "does it boot
at all." Everything beyond that, does paging actually work, does the disk
driver actually read what it wrote, does the context switch actually swap
stacks correctly, gets verified manually per change (see commit messages
and `docs/roadmap.md`'s per-item notes) against real QEMU-attached disks and
boot-time output, not just "it compiled."
